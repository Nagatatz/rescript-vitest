{
  "name": "@nagatatz/rescript-vitest",
  "version": "0.1.1",
  "description": "Type-safe ReScript bindings for Vitest — describe/test/expect matchers, vi mocks, spies, snapshots, fake timers.",
  "keywords": [
    "bindings",
    "mock",
    "rescript",
    "test",
    "testing",
    "vitest"
  ],
  "license": "MIT",
  "author": "Nagatatz <nagata.hbdc@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nagatatz/rescript-vitest.git"
  },
  "files": [
    "src",
    "rescript.json",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "scripts": {
    "build": "rescript build",
    "clean": "rescript clean",
    "watch": "rescript watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "format": "oxfmt '**/*.{js,mjs,cjs,json}'",
    "format:check": "oxfmt --check '**/*.{js,mjs,cjs,json}'",
    "lint": "oxlint",
    "prepare": "git config core.hooksPath .githooks || true",
    "prepublishOnly": "rescript build && vitest run"
  },
  "devDependencies": {
    "@rescript/language-server": "^1.72.0",
    "@rescript/runtime": "^12.3.0",
    "@vitest/coverage-v8": "^4.1.9",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "rescript": "^12.3.0",
    "vite": "^8.1.1",
    "vitest": "^4.1.9"
  },
  "peerDependencies": {
    "@rescript/runtime": "^12.0.0-0",
    "rescript": "^12.0.0-0",
    "vitest": "^4.0.0"
  },
  "packageManager": "pnpm@10.28.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
